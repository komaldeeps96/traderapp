# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# ── Node ───────────────────────────────────────────────
node_modules/
dist/
.vite/
*.tsbuildinfo

# ── Playwright ─────────────────────────────────────────
test-results/
playwright-report/
blob-report/
.playwright/

# ── Secrets & local state ──────────────────────────────
.env
.env.local
backend/config/settings.yaml
backend/config/state.yaml
# Exchange rates for periods that have already closed. A cache, not a
# setting: deleting it costs one refetch.
backend/config/fx-rates.json
# companyfacts kept by `pytest -m audit` so a second run costs the SEC
# nothing. Megabytes of XBRL, regenerated on demand.
backend/tests/audit/.cache/
backend/.logs/
.logs/

# ── Editors / OS ───────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── Backtesting data and models ────────────────────────
backtesting/

# ── Misc tooling ───────────────────────────────────────
opencode.json
skills-lock.json
.agents/
screenshot/
.screenshot/

# ── Pre-rewrite code, kept for reference. Safe to delete. ──
legacy/
*legacy-snapshot*.tar.gz

# ── Coverage ──
coverage/
coverage-e2e/
.coverage

frontend/dist-e2e
tmp/

# Claude Code's own directory. The skills under it are part of the repository —
# they are how the terminal's AI panels are worked on, and a contributor (or
# the CLI itself) should get them with a clone. Only the settings file is
# per-machine: it records which tools this laptop has approved.
.claude/settings.local.json
