/**
 * How loudly a scheduled report is flagged: red inside a week, amber inside a
 * fortnight, and nothing beyond two months. One set, so the top panel, the
 * swing screens and the watchlist call the same report by the same name.
 */
export const EARNINGS_URGENT_DAYS = 7;
export const EARNINGS_NEAR_DAYS = 14;
export const EARNINGS_HORIZON_DAYS = 60;
